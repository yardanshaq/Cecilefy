services:
  server:
    build:
      context: .
    volumes:
       - db_data_sqlite:/var/lib/cecilefy
       - custom:/cecilefy/custom
    environment:
      DB_FILENAME: "/var/lib/cecilefy/data.sqlite"
    ports:
      - 3000:3000
volumes:
  db_data_sqlite:
  custom:
